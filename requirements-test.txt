# Test dependencies for the translator package.
# test.bat installs ONLY this file, so pull in the runtime deps too.
-r requirements.txt

# Test framework
pytest==8.4.2
pytest-asyncio==1.2.0   # required by pytest.ini (asyncio_mode = auto)
pytest-cov==7.0.0       # required by test.bat (pytest --cov=translator)
